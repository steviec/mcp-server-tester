{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Evaluation Test Configuration",
  "description": "Schema for MCP evaluation test configuration files",
  "type": "object",
  "required": ["options", "tests"],
  "properties": {
    "options": {
      "type": "object",
      "required": ["models"],
      "properties": {
        "models": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          },
          "description": "List of LLM models to test with"
        },
        "timeout": {
          "type": "number",
          "minimum": 5000,
          "description": "Test timeout in milliseconds"
        },
        "max_steps": {
          "type": "number",
          "minimum": 1,
          "description": "Maximum number of conversation steps"
        }
      },
      "additionalProperties": false
    },
    "tests": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/test"
      },
      "description": "Array of evaluation tests to run"
    }
  },
  "additionalProperties": false,
  "definitions": {
    "test": {
      "type": "object",
      "required": ["name", "prompt"],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable test name"
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Prompt to send to the LLM"
        },
        "expected_tool_calls": {
          "type": "object",
          "properties": {
            "required": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Tools that must be called"
            },
            "allowed": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Tools that are allowed to be called (if not specified, all tools are allowed)"
            }
          },
          "additionalProperties": false,
          "description": "Tool call validation rules"
        },
        "response_scorers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/scorer"
          },
          "description": "Response quality scoring methods"
        }
      },
      "additionalProperties": false
    },
    "scorer": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["regex", "json-schema", "llm-judge"],
          "description": "Type of scorer to use"
        },
        "pattern": {
          "type": "string",
          "description": "Regex pattern to match (required for regex type)"
        },
        "schema": {
          "description": "JSON schema to validate against (required for json-schema type)"
        },
        "criteria": {
          "type": "string",
          "description": "Criteria for LLM judge (required for llm-judge type)"
        },
        "threshold": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Threshold for pass/fail (used with llm-judge)"
        }
      },
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "properties": { "type": { "const": "regex" } }
          },
          "then": {
            "required": ["pattern"]
          }
        },
        {
          "if": {
            "properties": { "type": { "const": "json-schema" } }
          },
          "then": {
            "required": ["schema"]
          }
        },
        {
          "if": {
            "properties": { "type": { "const": "llm-judge" } }
          },
          "then": {
            "required": ["criteria"]
          }
        }
      ]
    }
  }
}
