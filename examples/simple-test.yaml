# Simple tests without discovery
tests:
  - name: 'Echoes a basic test message'
    calls:
      - tool: 'echo'
        params:
          message: 'test'
        expect:
          success: true

options:
  timeout: 5000
