server_config: "./examples/server-config.json"

# Simple tests without discovery
tests:
  - name: "basic_echo"
    description: "Basic echo test"
    calls:
      - tool: "echo"
        params:
          message: "test"
        expect:
          success: true

options:
  timeout: 5000