# Simple tests without discovery
tests:
  - name: "basic_echo"
    calls:
      - tool: "echo"
        params:
          message: "test"
        expect:
          success: true

options:
  timeout: 5000